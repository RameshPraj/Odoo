# Copy to /etc/odoo/podman.env, make it readable only by the `odoo` service
# account, and never commit the copied file. This password must also be used in
# /etc/odoo/odoo.conf as db_password.
POSTGRES_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_VALUE
